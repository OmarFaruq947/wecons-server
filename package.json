{
  "name": "constructionServer",
  "version": "1.0.0",
  "description": "e-commerce",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch constructionServer.json --no-cors --port 5000"
  },
  "author": "omar faruq",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
